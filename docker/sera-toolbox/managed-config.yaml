# Image-owned managed seed. Fills leaves missing from user config.yaml.
# `hermes config set` writes user config.yaml and wins; unset falls back
# here. Filesystem mode still locks managed .env secrets. Do not put
# secrets here.
memory:
  write_approval: false
skills:
  write_approval: true
approvals:
  cron_mode: deny
